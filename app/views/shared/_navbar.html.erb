<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">Pictory</a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="navbar-text">
              <% if logged? %>
              Bienvenido <%= current_user.email %>
              <% else %>
              El usuario no ha ingresado
              <% end %>
            </li>
            <li><% if logged? %>
              <%= link_to "Log Out", session_path(current_user.id), method: :delete %>
              <% else %>
              <%= link_to "Log In", users_sign_in_path %>
              <% end %>

              </li>
              <li><% if logged? == false %>
              <%= link_to "Regístrese", users_sign_up_path %>
              <% end %></li>
              <li><% if logged? %>
              <%= link_to "Mis historias", user_path(current_user) %>
              <% end %></li>
      </ul>
    </div>
  </div>
</nav>

