<h3>New User</h3>

<%= form_with model: @user, local: true do |f| %>
	<% if @user.errors.any? %>
	<div id="error_explanation">
      <h2><%= pluralize(history.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
		<% @user.errors.full_messages.each do |messages| %>
			<li><%= message %></li>
		<% end %>
		</ul>
	</div>
	<% end %>

  <%= f.label :name %>
  <%= f.text_field :name, id: :user_name %>

  <%= f.label :email %>
  <%= f.text_field :email, id: :user_email %>

  <%= f.label :password %>
  <%= f.password_field :password, id: :user_password %>

  <%= f.submit "Crear Usuario" %>
<% end %>


